% —---------------------注释From ChatGPT-----------------
% 用于定制演示文稿中各个元素的颜色和外观。具体来说，它负责定制幻灯片中标题、正文、块、列表等不同部分的颜色、字体、背景色等方面的样式。
\mode<presentation>

    \definecolor{ZZUred}{HTML}{C32300} % main color!

    
% Settings
\setbeamercolor*{title page header}{fg=ZZUred}
\setbeamercolor*{author}{fg=black}
\setbeamercolor*{date}{fg=black}

\mode<all>


% —---------------------注释From ChatGPT-----------------
% 这是一段 LaTeX Beamer 文档的代码，用于设置演示文稿中文字和元素的颜色。下面对其中的 code 进行逐一解释：

% \mode<presentation> 声明当前模式为演示文稿模式。
% \definecolor{HFUTred}{HTML}{a8000c} 定义名为 HFUTred 的颜色变量，并将其值设置为 HTML 颜色代码 #a8000c，这个颜色作为主色调使用。
% \setbeamercolor*{title page header}{fg=HFUTred} 将演示文稿标题页的标题元素（通常包括标题、子标题）的前景色设置为 HFUTred，即 #a8000c。
% \setbeamercolor*{author}{fg=black} 将演示文稿作者信息的前景色设置为黑色。
% \setbeamercolor*{date}{fg=black} 将演示文稿日期的前景色设置为黑色。
% 最后的 \mode<all> 表示将所有内容都包含在演示文稿模式中。