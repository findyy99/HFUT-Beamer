% —---------------------注释-----------------
% 提供了一个主题为HFUT的幻灯片模版
\mode<presentation>

% Requirement
\RequirePackage{tikz}

% Settings
\useinnertheme{ZZU}
\useoutertheme{ZZU}
\usecolortheme{ZZU}

\setbeamertemplate{navigation symbols}{}

\mode<all>
% —---------------------注释From ChatGPT-----------------
% 这是一段 LaTeX Beamer 文档的代码，用于创建演示文稿。下面对其中的 code 进行逐一解释：

% \mode<presentation> 声明当前模式为演示文稿模式。
% \RequirePackage{tikz} 用于加载 TikZ 绘图包，以便在演示文稿中使用绘图功能。
% \useinnertheme{HFUT} 表示使用 HFUT 主题的内部样式（inner theme），主要指演示文稿中 frametitle、itemize 等元素的样式。
% \useoutertheme{HFUT} 表示使用 HFUT 主题的外部样式（outer theme），主要指演示文稿中页眉、页脚等元素的样式。
% \usecolortheme{HFUT} 表示使用 HFUT 主题的颜色样式（color theme），主要指演示文稿中文字和其他元素的颜色。
% \setbeamertemplate{navigation symbols}{} 去除演示文稿中默认的导航栏。
% 最后的 \mode<all> 表示将所有内容都包含在演示文稿模式中。